# Router CRUD

Простое React-приложение для управления постами: создание, просмотр, редактирование и удаление. Использует React Router для навигации и JSON-сервер в качестве бэкенда.

## Запуск

```bash
npm install
npm run dev
```

Бэкенд запускается отдельно:

```bash
cd backend
npm run start
```
